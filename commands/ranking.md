---
description: Show the aurabr.xyz aura ranking in the terminal
argument-hint: "[--kind startup|vc|college] [--limit 20]"
---

# /aura:ranking

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/aura.mjs" ranking $ARGUMENTS
```

Echo the output verbatim in a fenced code block. It is already styled and
ranked. Add at most one line of commentary — the user asked for a ranking, not
an analysis. If they ask a follow-up about a specific company, then dig in.
